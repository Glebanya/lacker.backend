overblog_graphql:
    definitions:
        schema:
            public:
                query: Public
            private:
                query: Private
        mappings:
            types:
                -
                    type: yaml
                    dir: "%kernel.project_dir%/config/graphql/types"
                    suffix: ~
